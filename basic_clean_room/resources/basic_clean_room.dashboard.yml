resources:
	dashboards:
		clean_room_sample_data_bi:
			display_name: "Healthcare Visit Analytics"
			warehouse_id: ${resources.warehouses.aibi_warehouse.id}
			file_path: "../src/healthcare_visit_analytics.lvdash.json"
			embed_credentials: true
			dataset_catalog: ${var.catalog}
			dataset_schema: ${resources.schemas.basic_clean_room_schema.name}